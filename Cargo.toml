[package]
name = "owiwi"
version = "0.1.0"
edition = "2024"
license = "MIT"

[features]
clap = ["dep:clap"]

[dependencies]
clap = { version = "4.5.48", optional = true, features = ["derive"] }

[dev-dependencies]
googletest = { version = "0.14.2", features = ["proptest"] }
proptest = "1.8.0"
rstest = "0.26.1"
